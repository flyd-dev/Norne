# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# environment files — NEVER commit real secrets
.env
.env*.local
.env.development
.env.production

# Local credential documentation — contains real secrets, must not be committed.
# Keep credentials in .env.local only.
api-service-account.md

# Internal strategy doc (Brakka) — not part of the chatbot, keep out of the repo.
CLAUDE.md
