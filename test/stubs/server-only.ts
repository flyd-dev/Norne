// Test stub for the `server-only` package. In production this import marks a
// module as server-only and throws if pulled into a client bundle. For unit
// tests we replace it with a no-op so server modules can be imported directly.
export {};
